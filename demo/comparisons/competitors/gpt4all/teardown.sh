#!/usr/bin/env bash
set -euo pipefail

# GPT4All teardown — desktop app, nothing to clean
echo "=== GPT4All Teardown ==="
echo "GPT4All is a desktop app. Close it manually if running."
echo "To remove LocalDocs index: Settings → LocalDocs → Remove folder"
